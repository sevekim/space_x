import 'package:flutter/material.dart';

/// **********************************************************************
///
///  Custom Padding
///
/// **********************************************************************/

const commonScaffoldAllPadding = EdgeInsets.all(
  15,
);

const commonNextWidgetPadding = EdgeInsets.only(
  bottom: 15,
);

const commonVerticalNextWidgetPadding = EdgeInsets.only(
  top: 20,
  bottom: 8.0,
  left: 5.0,
);

const commonSistersWidgetPaddingSmall = EdgeInsets.only(
  bottom: 6,
);

const commonSistersWidgetPadding = EdgeInsets.only(
  bottom: 8,
);

const commonScaffoldLTRPadding = EdgeInsets.only(
  left: 15,
  top: 15,
  right: 15,
);

const commonScaffoldScrollablePadding = EdgeInsets.symmetric(
  horizontal: 15,
  vertical: 15.0,
);

const commonSingleListPadding = EdgeInsets.symmetric(
  horizontal: 10,
);

const commonSingleModalInfoPadding = EdgeInsets.only(
  top: 10.0,
  bottom: 8,
);

const commonSingleModalPanelPadding = EdgeInsets.symmetric(
  vertical: 10.0,
);
